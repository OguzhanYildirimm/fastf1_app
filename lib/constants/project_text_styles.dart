import 'package:flutter/material.dart';

class ProjectTextStyles {
  TextStyle titleTextStyle =
      const TextStyle(fontFamily: 'Formula1-Bold', fontSize: 24);

  TextStyle scheduleTitleTextStyle =
      const TextStyle(fontFamily: 'Formula1-Regular', fontSize: 16);

  TextStyle scheduleDetailTitleTextStyle =
      const TextStyle(fontFamily: 'Formula1-Bold', fontSize: 18);

  TextStyle scheduleDetailCircuitNameTextStyle =
      const TextStyle(fontFamily: 'Formula1-Bold', fontSize: 16);

  TextStyle scheduleDetailSessionTitleTextStyle = TextStyle(
      fontFamily: 'Formula1-Bold', fontSize: 18, color: Colors.grey[600]);

  TextStyle scheduleDetailSessionDateTextStyle = const TextStyle(
      fontFamily: 'Formula1-Wide', fontSize: 14, color: Colors.black);

  TextStyle scheduleDetailOfficialEventNameTextStyle =
      const TextStyle(fontFamily: 'Formula1-Bold', fontSize: 18);

  TextStyle scheduleSubTitleTextStyle =
      const TextStyle(fontFamily: 'Formula1-Bold', fontSize: 14);

  TextStyle scheduleDetailSessionTextStyle =
      const TextStyle(fontFamily: 'Formula1-Regular', fontSize: 14);

  TextStyle buttonTextStyle =
      const TextStyle(fontFamily: 'Formula1-Regular', fontSize: 18);

  TextStyle driverNameTextStyle =
      const TextStyle(fontFamily: 'Formula1-Regular', fontSize: 16);

  TextStyle driverNumberTextStyle =
      const TextStyle(fontFamily: 'Formula1-Bold', fontSize: 18);

  TextStyle driverTeamTextStyle =
      const TextStyle(fontFamily: 'Formula1-Bold', fontSize: 16);
}
